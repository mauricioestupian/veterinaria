Paquetes:
-- Para manejo de Rutas
npm install react-router-dom

--librería de íconos para aplicaciones React que convierte cada ícono en un componente tipado que renderiza un SVG optimizado
npm install lucide-react

-- estilos de tailwind
npm install -D tailwindcss postcss autoprefixer
npm install -D @tailwindcss/postcss
crear archivo en raiz postcss.config.js
export default {
plugins: {
"@tailwindcss/postcss": {},
},
}

--
npm install react-hook-form zod @hookform/resolvers
